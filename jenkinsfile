pipeline {
           agent any
           stages {
                stage("Hello") {
                     steps {
                          currentBuild.displayName = "${env.GIT_COMMIT}"
                          echo "Estoy cambiando el nombre"
                     }
                }
                 stage("TERRA") {
                     steps {
                          sh 'terraform init'
                          sh 'terrafor plan'
                     }
                }
           }
      }
